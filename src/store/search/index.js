// search模块小仓库
// state:仓库存储数据的地方
const state ={
};
//mutations:修改state的唯一手段
const mutations = {
};
//actions:处理异步，处理业务
const actions = {
};
//getters:类似计算属性，用于简化仓库数据，让组件获取仓库的数据更加方便。
const getters = {};
export default {
    state,
    mutations,
    actions,
    getters
}